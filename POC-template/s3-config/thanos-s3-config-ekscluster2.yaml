type: S3
prefix: "${CLUSTER_NAME_2}"
config:
    bucket: "${THANOS_BUCKET_NAME}"
    endpoint: "s3.${AWS_DEFAULT_REGION}.amazonaws.com"
    region: "${AWS_DEFAULT_REGION}"
    sts_endpoint: "https://sts.amazonaws.com"
